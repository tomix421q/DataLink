import Motion from "./motion.svelte";
import { updateLazyFeatures } from "../../features/lazy-features.js";
import { withProp } from "../../vendor/runed/index.js";
const componentMaxCache = new Map();
const componentMiniCache = new Map();
export function createMotionComponent(component, options = {}) {
    const isString = typeof component === "string";
    const name = isString ? component : component.name || "";
    const componentCache = options.renderer ? componentMaxCache : componentMiniCache;
    if (isString && componentCache?.has(component)) {
        return componentCache.get(component);
    }
    const motionComponent = (anchor, props) => {
        const getAs = () => props.as || component || "div";
        return Motion(anchor, {
            renderer: options.renderer,
            get forwardMotionProps() {
                return props.forwardMotionProps || options.forwardMotionProps;
            },
            get as() {
                return getAs();
            },
            get props() {
                return withProp(props, "as", getAs());
            },
            get ref() {
                return props.ref;
            },
            set ref(value) {
                props.ref = value;
            },
        });
    };
    if (isString) {
        componentCache?.set(component, motionComponent);
    }
    return motionComponent;
}
export function createMotionComponentWithFeatures(featureBundle) {
    const renderer = featureBundle?.renderer;
    updateLazyFeatures(featureBundle?.features || []);
    return new Proxy({}, {
        get(_target, key) {
            if (key === "create") {
                return (component, options) => createMotionComponent(component, {
                    ...options,
                    renderer,
                });
            }
            return createMotionComponent(key, {
                renderer,
            });
        },
    });
}
